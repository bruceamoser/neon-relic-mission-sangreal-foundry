# Sangreal scene art — approved and integrated

26 images: 16 theater-of-the-mind environments and 10 gridless battle maps. User approved all 26 images; integrated into the Mission Scenes pack and existing GM documents.

Open `index.html` to review full-resolution images and record/export notes. Runtime WebP q85 assets total 6.55 MB, at native generated dimensions without resizing. Lossless masters are preserved separately and omitted from the review ZIP.

Use `assets/art/scenes/totm` and `assets/art/scenes/maps` as Foundry backgrounds. The same files are installed in the repository's `src/assets/art/scenes` and copied into the handoff `final-art/assets/art/scenes`. Building the module copies these compressed source assets.

These backgrounds now have configured gridless Foundry Scene documents in src/packs/sangreal-scenes.yaml. Set width/height to the manifest dimensions. Maps have no baked grid; calibrate token scale manually. Add walls, lights, elevations and encounter tokens after visual approval. Do not use draft requested pixel dimensions as actual dimensions. Interiors reveal floor plans: show only at appropriate GM timing.

The factory floor and catwalk share a footprint; other maps are individual playable interpretations. Photos communicate atmosphere and are not exact floor-plan surveys. The clinic map depicts the third floor, with surrounding foliage for context only. Abbey map illumination prioritizes tactical readability.

Created with built-in ImageGen. Full generation and revision prompts are in the production generation log and prompts folder. `manifest.json` records sources, hashes, reveal timing, actual dimensions and file sizes.
